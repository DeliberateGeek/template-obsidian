# Canonical Metadata

> Auto-generated rendered view of `vault-metadata.yaml`. Do not edit directly — update the YAML source and regenerate.

## Content Types

```dataview
TABLE WITHOUT ID
  id AS "Type",
  description AS "Description",
  folder AS "Folder",
  also_tag AS "Also Tag?"
FROM "🫥 Meta"
WHERE file.name = "vault-metadata"
FLATTEN content_types AS ct
FLATTEN ct.id AS id
FLATTEN ct.description AS description
FLATTEN ct.folder AS folder
FLATTEN ct.also_tag AS also_tag
```

> **Note:** The dataview query above is a placeholder. Dataview cannot natively parse YAML data files — the actual rendered view will be generated by Claude or the `/audit-metadata` Skill by reading `vault-metadata.yaml` and producing a markdown table.

## Topics

| Tag | Aliases | Deprecated |
|-----|---------|:----------:|
| *Populated during Phase 1 seed* | | |

## Properties

| Name | Type | Cardinality | Description |
|------|------|-------------|-------------|
| status | enum | single | Lifecycle state |
| type | string | single | Mirrors content_type id |
| created | date | single | Note creation date |
| updated | date | single | Last modification date |
| metadata_review | enum | single | Deferred review flag |
| metadata_review_note | string | single | Reason for deferral |

## Recent Audit Activity

```dataview
TABLE WITHOUT ID
  file.name AS "Date",
  length(file.lists) AS "Entries"
FROM "🫥 Meta/Audit Logs"
WHERE file.name != ".gitkeep"
SORT file.name DESC
LIMIT 30
```

## Deprecated

### Tags
*None yet*

### Properties
*None yet*
